import React from 'react';
import ReactDOM from 'react-dom/client';
import KevinKiruiPortfolio from './Portfolio';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <KevinKiruiPortfolio />
  </React.StrictMode>,
);
